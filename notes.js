// pegs, peg-console, peg boolean
// buttons: start, pegs "onclick", submit row
// show ratio of correct, misplaced, and false

// global variables
// master peg colors
// availablse colors

// const colors = ['green', 'blue', 'red']

// function shuffle(array) {
//     let currentIndex = array.length,  randomIndex;
  
//     // While there remain elements to shuffle.
//     while (currentIndex != 0) {
  
//       // Pick a remaining element.
//       randomIndex = Math.floor(Math.random() * currentIndex);
//       currentIndex--;
  
//       // And swap it with the current element.
//       [array[currentIndex], array[randomIndex]] = [
//         array[randomIndex], array[currentIndex]];
//     }
  
//     return array;
//   }

// function main() {
//     const masterColors = shuffle(colors)
//     console.log(masterColors)

//     const gamePegs = document.querySelectorAll('.dot')
//     Array.from(gamePegs).forEach(peg => {
//         console.log(peg)
//     })
// }

// main()